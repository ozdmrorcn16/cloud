# Faz 1 — Hesap Uygulama Plani

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Kullanicinin telefon numarasi + sifreyle kayit olup SMS ile dogrulayabildigi,
18 yas alti reddedilen, profil (ad, dogum tarihi, biyografi, fotograf) olusturabildigi
ve oturumu kapatip acabildigi calisan bir Expo uygulamasi. Sonunda kullanici hesap
acabiliyor ama henuz kimseyi gormuyor — kesif Faz 2'de.

**Architecture:** Expo (React Native + TypeScript, Expo Router) istemcisi, Supabase
(Auth + Postgres + Storage) backend'i. Kimlik dogrulama tamamen Supabase Auth'un
telefon+sifre akisina devredilir; uygulama kodu sadece formlari ve yonlendirmeyi
yazar. Gelistirme boyunca gercek SMS gonderilmez — Supabase'in ucretsiz "test
telefon numaralari" ozelligi kullanilir; gercek saglayici (Twilio) devreye alma
adimi ayri ve izole bir gorevde (Task 14), magazaya cikmadan hemen once calistirilir.

**Tech Stack:** Expo (latest SDK, `create-expo-app` varsayilan sablonu — Expo
Router dahil), TypeScript (strict), `@supabase/supabase-js` v2 (formlar duz
React state ile yonetilir, ayri bir form kutuphanesi yok),
`@react-native-async-storage/async-storage`, `expo-image-picker`,
`expo-file-system` + `base64-arraybuffer` (Storage yuklemesi icin), Jest
(`jest-expo` preset) + React Native Testing Library, Supabase CLI (yalnizca
migrasyon linkleme icin, yerel Docker gerekmez).

## Global Constraints

- Node.js 18+ (mevcut ortamda zaten var).
- TypeScript strict mode acik.
- Paket yoneticisi: npm.
- UI dili: Turkce.
- Telefon numaralari uygulama icinde her zaman E.164 formatinda tutulur (`+90...`).
- Sifre: minimum 8 karakter.
- Yas siniri: kayitta dogum tarihinden hesaplanan yas < 18 ise kayit reddedilir.
  Yas bandi veya veli onayi **yok** (2026-08-12 karari, spec guncellendi).
- SMS: Faz 1 boyunca Supabase'in "test OTP" numaralariyla ucretsiz test edilir.
  Gercek saglayici entegrasyonu (Twilio) yalnizca Task 14'te, izole sekilde
  devreye alinir.
- Supabase projesi: hosted ucretsiz katman (Free tier), yerel Docker stack'i
  **kullanilmiyor** — Docker Desktop kurulumu bu fazda gerekmiyor.

---

### Task 1: Proje iskeleti

**Files:**
- Create: `mobil/` (Expo projesi, repo koku altinda)
- Create: `mobil/package.json`, `mobil/tsconfig.json`, `mobil/app.json`
- Create: `mobil/jest.config.js`
- Modify: repo `.gitignore` (mobil/node_modules, mobil/.env eklenir)

**Interfaces:**
- Produces: `mobil/` altinda calisan bir Expo Router iskeleti; sonraki tum
  gorevler bu klasorun icine yazar.

- [ ] **Step 1: Expo projesini olustur**

```bash
cd ~/projects/cloud
npx create-expo-app@latest mobil --template default
cd mobil
```

- [ ] **Step 2: Test altyapisini kur**

```bash
npx expo install jest-expo jest @types/jest
npm install --save-dev @testing-library/react-native @testing-library/jest-native
```

`mobil/package.json` icine ekle:

```json
{
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "preset": "jest-expo"
  }
}
```

- [ ] **Step 3: Ornek testin calistigini dogrula**

`mobil/lib/ornek.test.ts` gecici dosyasi:

```ts
test('jest calisiyor', () => {
  expect(1 + 1).toBe(2)
})
```

Run: `cd mobil && npm test`
Expected: PASS, 1 test.

Dogrulandiktan sonra `mobil/lib/ornek.test.ts` dosyasini sil.

- [ ] **Step 4: Repo .gitignore'u guncelle**

Repo kokundeki `.gitignore` dosyasina ekle (yoksa olustur):

```
mobil/node_modules/
mobil/.env
mobil/.expo/
```

- [ ] **Step 5: Uygulamanin bos halde calistigini dogrula**

Run: `cd mobil && npx expo start --web`
Expected: Metro bundler acilir, tarayicida Expo Router varsayilan ekrani gorunur.
Sunucuyu durdur (Ctrl+C).

- [ ] **Step 6: Commit**

```bash
cd ~/projects/cloud
git add mobil .gitignore
git commit -m "mobil: Expo + TypeScript + Expo Router iskeleti"
```

---

### Task 2: Supabase projesi ve CLI baglantisi

**Files:**
- Create: `mobil/supabase/config.toml` (CLI tarafindan uretilir)
- Create: `mobil/.env.example`
- Create: `mobil/.env` (gitignored, gercek degerler)

**Interfaces:**
- Produces: `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY` ortam
  degiskenleri — Task 4'te Supabase istemcisi bunlari okur.
- Produces: linklenmis bir Supabase projesi — Task 3'teki migrasyon buna
  uygulanir.

- [ ] **Step 1: Supabase hesabi ve ucretsiz proje**

<https://supabase.com> uzerinde ucretsiz hesap ac (yoksa), "New Project" ile
yeni proje olustur. Bolge olarak Avrupa'ya en yakin bolgeyi sec (ornegin
`eu-central-1`). Veritabani sifresini not al (CLI linklemede lazim).

- [ ] **Step 2: Supabase CLI'yi kur**

```bash
npm install -g supabase
supabase --version
```

- [ ] **Step 3: Projeyi CLI ile baslat ve linkle**

```bash
cd ~/projects/cloud/mobil
supabase init
supabase login
supabase link --project-ref <dashboard'daki-project-ref>
```

`project-ref`, Supabase dashboard'da proje ayarlarinin URL'sinde gorunur
(`https://supabase.com/dashboard/project/<project-ref>`).

- [ ] **Step 4: Ortam degiskenlerini yaz**

Dashboard → Project Settings → API sayfasindan `Project URL` ve `anon public`
anahtarini al.

`mobil/.env.example`:

```
EXPO_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJ...
```

`mobil/.env` (gercek degerlerle, commit edilmez):

```
EXPO_PUBLIC_SUPABASE_URL=<gercek-url>
EXPO_PUBLIC_SUPABASE_ANON_KEY=<gercek-anon-key>
```

- [ ] **Step 5: Dogrula**

Run: `cat mobil/.env` (degerlerin dolu oldugunu goz kontrolu ile dogrula)
Run: `supabase projects list` → linklenen proje listede gorunmeli.

- [ ] **Step 6: Commit**

```bash
cd ~/projects/cloud
git add mobil/.env.example mobil/supabase
git commit -m "mobil: Supabase projesi linklendi, ortam degiskeni sablonu eklendi"
```

(`.env` zaten gitignore'da, commit edilmeyecek.)

---

### Task 3: `profiller` tablosu migrasyonu ve RLS

**Files:**
- Create: `mobil/supabase/migrations/<timestamp>_profiller.sql`

**Interfaces:**
- Produces: `public.profiller` tablosu — Task 10 ve Task 11 bu tabloya yazar.
- Not: Spec'teki veri modeli taslaginda `telefon_dogrulandi` ve
  `checkin_gorunurlugu` alanlari da vardi. `telefon_dogrulandi`,
  `auth.users.phone_confirmed_at` ile birebir tekrar edecegi icin buraya
  eklenmiyor (DRY) — ihtiyac olan yerde canli sorgulanir. `checkin_gorunurlugu`
  Faz 2'nin (check-in) konusu; o faz kendi migrasyonunda ekleyecek (YAGNI).

- [ ] **Step 1: Migrasyon dosyasini olustur**

```bash
cd ~/projects/cloud/mobil
supabase migration new profiller
```

Bu, `mobil/supabase/migrations/<timestamp>_profiller.sql` dosyasini olusturur.

- [ ] **Step 2: Tablo ve RLS'i yaz**

Olusturulan dosyaya:

```sql
create table public.profiller (
  id uuid primary key references auth.users(id) on delete cascade,
  ad text not null,
  dogum_tarihi date not null,
  biyografi text,
  fotograflar text[] not null default '{}',
  olusturuldu timestamptz not null default now()
);

alter table public.profiller enable row level security;

create policy "kendi profilini okuyabilir"
  on public.profiller for select
  using (auth.uid() = id);

create policy "kendi profilini olusturabilir"
  on public.profiller for insert
  with check (auth.uid() = id);

create policy "kendi profilini guncelleyebilir"
  on public.profiller for update
  using (auth.uid() = id);
```

- [ ] **Step 3: Migrasyonu uzak projeye uygula**

```bash
supabase db push
```

Expected: "Applying migration ... profiller.sql" ciktisi, hata yok.

- [ ] **Step 4: RLS'i dogrula**

Dashboard → Table Editor → `profiller` tablosunda "RLS enabled" etiketi
gorunmeli. Dashboard → SQL Editor'de asagidaki sorguyu calistir:

```sql
select tablename, rowsecurity from pg_tables where tablename = 'profiller';
```

Expected: `rowsecurity = true`.

- [ ] **Step 5: Commit**

```bash
cd ~/projects/cloud
git add mobil/supabase/migrations
git commit -m "mobil: profiller tablosu ve RLS politikalari"
```

---

### Task 4: Supabase istemcisi ve oturum saklama adaptoru

**Files:**
- Create: `mobil/lib/supabase.ts`
- Test: `mobil/lib/supabase.test.ts`

**Interfaces:**
- Consumes: `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY` (Task 2).
- Produces: `supabase` (Supabase `SupabaseClient` instance) — Task 6, 7, 8, 9,
  10, 11 tarafindan import edilir.

- [ ] **Step 1: Bagimliliklari kur**

```bash
cd ~/projects/cloud/mobil
npx expo install @supabase/supabase-js @react-native-async-storage/async-storage react-native-url-polyfill
```

- [ ] **Step 2: Basarisiz testi yaz**

`mobil/lib/supabase.test.ts`:

```ts
import { supabase } from './supabase'

test('supabase istemcisi olusturulmus', () => {
  expect(supabase).toBeDefined()
  expect(typeof supabase.auth.signUp).toBe('function')
})
```

- [ ] **Step 3: Testi calistir, basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- supabase.test.ts`
Expected: FAIL — `./supabase` module bulunamiyor.

- [ ] **Step 4: Istemciyi yaz**

`mobil/lib/supabase.ts`:

```ts
import 'react-native-url-polyfill/auto'
import AsyncStorage from '@react-native-async-storage/async-storage'
import { createClient } from '@supabase/supabase-js'
import { AppState } from 'react-native'

const supabaseUrl = process.env.EXPO_PUBLIC_SUPABASE_URL!
const supabaseAnonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!

export const supabase = createClient(supabaseUrl, supabaseAnonKey, {
  auth: {
    storage: AsyncStorage,
    autoRefreshToken: true,
    persistSession: true,
    detectSessionInUrl: false,
  },
})

AppState.addEventListener('change', (durum) => {
  if (durum === 'active') {
    supabase.auth.startAutoRefresh()
  } else {
    supabase.auth.stopAutoRefresh()
  }
})
```

- [ ] **Step 5: Testi tekrar calistir**

Run: `cd mobil && npm test -- supabase.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
cd ~/projects/cloud
git add mobil/lib/supabase.ts mobil/lib/supabase.test.ts mobil/package.json mobil/package-lock.json
git commit -m "mobil: Supabase istemcisi ve oturum saklama adaptoru"
```

---

### Task 5: Dogrulama yardimci fonksiyonlari (yas, telefon formati)

**Files:**
- Create: `mobil/lib/yas.ts`
- Test: `mobil/lib/yas.test.ts`
- Create: `mobil/lib/telefon.ts`
- Test: `mobil/lib/telefon.test.ts`

**Interfaces:**
- Produces: `hesaplaYas(dogumTarihi: Date, bugun?: Date): number`,
  `onSekizAltindaMi(dogumTarihi: Date, bugun?: Date): boolean` — Task 10 kayit
  formunda kullanir.
- Produces: `eFormatinaCevir(girilenNumara: string): string | null` — Task 6 ve
  Task 8 formlarinda kullanir. `null` donerse numara gecersizdir.

- [ ] **Step 1: Yas testlerini yaz**

`mobil/lib/yas.test.ts`:

```ts
import { hesaplaYas, onSekizAltindaMi } from './yas'

describe('hesaplaYas', () => {
  it('dogum gunu henuz gelmediyse bir yas dusuk hesaplar', () => {
    const bugun = new Date('2026-08-13')
    const dogum = new Date('2008-08-14')
    expect(hesaplaYas(dogum, bugun)).toBe(17)
  })

  it('dogum gunu bugunse yeni yasi sayar', () => {
    const bugun = new Date('2026-08-13')
    const dogum = new Date('2008-08-13')
    expect(hesaplaYas(dogum, bugun)).toBe(18)
  })
})

describe('onSekizAltindaMi', () => {
  it('17 yasindaki kullaniciyi reddeder', () => {
    const bugun = new Date('2026-08-13')
    expect(onSekizAltindaMi(new Date('2009-01-01'), bugun)).toBe(true)
  })

  it('18 yasindaki kullaniciyi kabul eder', () => {
    const bugun = new Date('2026-08-13')
    expect(onSekizAltindaMi(new Date('2008-01-01'), bugun)).toBe(false)
  })
})
```

- [ ] **Step 2: Basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- yas.test.ts`
Expected: FAIL — `./yas` module bulunamiyor.

- [ ] **Step 3: Yas modulunu yaz**

`mobil/lib/yas.ts`:

```ts
export function hesaplaYas(dogumTarihi: Date, bugun: Date = new Date()): number {
  let yas = bugun.getFullYear() - dogumTarihi.getFullYear()
  const dogumGunuGecti =
    bugun.getMonth() > dogumTarihi.getMonth() ||
    (bugun.getMonth() === dogumTarihi.getMonth() && bugun.getDate() >= dogumTarihi.getDate())
  if (!dogumGunuGecti) yas -= 1
  return yas
}

export function onSekizAltindaMi(dogumTarihi: Date, bugun: Date = new Date()): boolean {
  return hesaplaYas(dogumTarihi, bugun) < 18
}
```

- [ ] **Step 4: Yas testlerinin gectigini dogrula**

Run: `cd mobil && npm test -- yas.test.ts`
Expected: PASS, 4 test.

- [ ] **Step 5: Telefon testlerini yaz**

`mobil/lib/telefon.test.ts`:

```ts
import { eFormatinaCevir } from './telefon'

describe('eFormatinaCevir', () => {
  it('basinda 0 olan 11 haneli numarayi +90 formatina cevirir', () => {
    expect(eFormatinaCevir('05551234567')).toBe('+905551234567')
  })

  it('basinda 0 olmayan 10 haneli numarayi +90 formatina cevirir', () => {
    expect(eFormatinaCevir('5551234567')).toBe('+905551234567')
  })

  it('boslukli ve tireli girisi temizler', () => {
    expect(eFormatinaCevir('0555 123 45 67')).toBe('+905551234567')
  })

  it('gecersiz uzunluktaki numara icin null doner', () => {
    expect(eFormatinaCevir('12345')).toBeNull()
  })

  it('zaten +90 ile baslayan numarayi oldugu gibi kabul eder', () => {
    expect(eFormatinaCevir('+905551234567')).toBe('+905551234567')
  })
})
```

- [ ] **Step 6: Basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- telefon.test.ts`
Expected: FAIL — `./telefon` module bulunamiyor.

- [ ] **Step 7: Telefon modulunu yaz**

`mobil/lib/telefon.ts`:

```ts
export function eFormatinaCevir(girilenNumara: string): string | null {
  const temiz = girilenNumara.replace(/[\s-]/g, '')

  if (temiz.startsWith('+90') && temiz.length === 13) {
    return temiz
  }
  if (temiz.startsWith('0') && temiz.length === 11) {
    return `+90${temiz.slice(1)}`
  }
  if (!temiz.startsWith('0') && !temiz.startsWith('+') && temiz.length === 10) {
    return `+90${temiz}`
  }
  return null
}
```

- [ ] **Step 8: Tum yardimci fonksiyon testlerinin gectigini dogrula**

Run: `cd mobil && npm test -- yas.test.ts telefon.test.ts`
Expected: PASS, 9 test.

- [ ] **Step 9: Commit**

```bash
cd ~/projects/cloud
git add mobil/lib/yas.ts mobil/lib/yas.test.ts mobil/lib/telefon.ts mobil/lib/telefon.test.ts
git commit -m "mobil: yas hesaplama ve telefon formati dogrulama yardimcilari"
```

---

### Task 6: Kayit ekrani (telefon + sifre) — TAMAMLANDI

> Bu bolum, uygulanmis haliyle guncellendi (asagidaki iki not) — implementer
> bu iki sapmayi kesfetti, sonraki tum gorevler (7-12) buna gore yazildi.

**Files:**
- Create: `mobil/src/app/(auth)/kayit.tsx`
- Test: `mobil/src/app/(auth)/kayit.test.tsx`

**Interfaces:**
- Consumes: `supabase` (Task 4), `eFormatinaCevir` (Task 5).
- Produces: basarili kayittan sonra `/dogrula?telefon=<eFormatli>` rotasina
  yonlendirme — Task 7 bu parametreyi okur.

**Not:** Router koku `mobil/app/` degil `mobil/src/app/` (create-expo-app
sablonu boyle olusturdu) — dosya yolu ve `../../../lib/...` import derinligi
buna gore. `@testing-library/react-native@14.0.1`'de `render()`/`fireEvent.*`
async — asagida `await` var.

- [ ] **Step 1: Basarisiz testi yaz**

`mobil/src/app/(auth)/kayit.test.tsx`:

```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react-native'
import KayitEkrani from './kayit'
import { supabase } from '../../../lib/supabase'

jest.mock('../../../lib/supabase', () => ({
  supabase: { auth: { signUp: jest.fn() } },
}))

const mockRouterPush = jest.fn()
jest.mock('expo-router', () => ({
  useRouter: () => ({ push: mockRouterPush }),
}))

describe('KayitEkrani', () => {
  it('gecersiz telefon numarasinda hata gosterir', async () => {
    await render(<KayitEkrani />)
    await fireEvent.changeText(screen.getByPlaceholderText('05XX XXX XX XX'), '123')
    await fireEvent.changeText(screen.getByPlaceholderText('Sifre'), 'sifre1234')
    await fireEvent.press(screen.getByText('Kayit ol'))
    await waitFor(() => {
      expect(screen.getByText('Gecerli bir telefon numarasi gir')).toBeTruthy()
    })
    expect(supabase.auth.signUp).not.toHaveBeenCalled()
  })

  it('gecerli bilgilerle signUp cagirir ve dogrulama ekranina yonlendirir', async () => {
    ;(supabase.auth.signUp as jest.Mock).mockResolvedValue({ data: {}, error: null })
    await render(<KayitEkrani />)
    await fireEvent.changeText(screen.getByPlaceholderText('05XX XXX XX XX'), '5551234567')
    await fireEvent.changeText(screen.getByPlaceholderText('Sifre'), 'sifre1234')
    await fireEvent.press(screen.getByText('Kayit ol'))
    await waitFor(() => {
      expect(supabase.auth.signUp).toHaveBeenCalledWith({
        phone: '+905551234567',
        password: 'sifre1234',
      })
    })
    expect(mockRouterPush).toHaveBeenCalledWith('/dogrula?telefon=%2B905551234567')
  })
})
```

- [ ] **Step 2: Basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- kayit.test.tsx`
Expected: FAIL — `./kayit` module bulunamiyor.

- [ ] **Step 3: Kayit ekranini yaz**

`mobil/src/app/(auth)/kayit.tsx`:

```tsx
import { useState } from 'react'
import { View, Text, TextInput, Pressable, StyleSheet } from 'react-native'
import { useRouter } from 'expo-router'
import { supabase } from '../../../lib/supabase'
import { eFormatinaCevir } from '../../../lib/telefon'

export default function KayitEkrani() {
  const router = useRouter()
  const [telefon, setTelefon] = useState('')
  const [sifre, setSifre] = useState('')
  const [hata, setHata] = useState<string | null>(null)
  const [gonderiliyor, setGonderiliyor] = useState(false)

  async function kayitOl() {
    setHata(null)
    const eFormatli = eFormatinaCevir(telefon)
    if (!eFormatli) {
      setHata('Gecerli bir telefon numarasi gir')
      return
    }
    if (sifre.length < 8) {
      setHata('Sifre en az 8 karakter olmali')
      return
    }

    setGonderiliyor(true)
    const { error } = await supabase.auth.signUp({ phone: eFormatli, password: sifre })
    setGonderiliyor(false)

    if (error) {
      setHata(error.message)
      return
    }
    router.push(`/dogrula?telefon=${encodeURIComponent(eFormatli)}`)
  }

  return (
    <View style={stiller.kapsayici}>
      <Text style={stiller.baslik}>Hesap olustur</Text>
      <TextInput
        style={stiller.girdi}
        placeholder="05XX XXX XX XX"
        keyboardType="phone-pad"
        value={telefon}
        onChangeText={setTelefon}
      />
      <TextInput
        style={stiller.girdi}
        placeholder="Sifre"
        secureTextEntry
        value={sifre}
        onChangeText={setSifre}
      />
      {hata && <Text style={stiller.hata}>{hata}</Text>}
      <Pressable style={stiller.buton} onPress={kayitOl} disabled={gonderiliyor}>
        <Text style={stiller.butonYazi}>{gonderiliyor ? 'Gonderiliyor...' : 'Kayit ol'}</Text>
      </Pressable>
    </View>
  )
}

const stiller = StyleSheet.create({
  kapsayici: { flex: 1, padding: 24, justifyContent: 'center' },
  baslik: { fontSize: 24, fontWeight: '600', marginBottom: 24 },
  girdi: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12, marginBottom: 12 },
  hata: { color: '#c00', marginBottom: 12 },
  buton: { backgroundColor: '#111', borderRadius: 8, padding: 14, alignItems: 'center' },
  butonYazi: { color: '#fff', fontWeight: '600' },
})
```

- [ ] **Step 4: Testlerin gectigini dogrula**

Run: `cd mobil && npm test -- kayit.test.tsx`
Expected: PASS, 2 test.

- [ ] **Step 5: Commit**

```bash
cd ~/projects/cloud
git add mobil/src/app/\(auth\)/kayit.tsx mobil/src/app/\(auth\)/kayit.test.tsx
git commit -m "mobil: kayit ekrani (telefon + sifre)"
```

---

### Task 7: SMS dogrulama ekrani

**Files:**
- Create: `mobil/src/app/(auth)/dogrula.tsx`
- Test: `mobil/src/app/(auth)/dogrula.test.tsx`

**Interfaces:**
- Consumes: `supabase` (Task 4), URL parametresi `telefon` (Task 6'dan gelir).
- Produces: basarili dogrulamadan sonra `/profil-olustur` rotasina yonlendirme
  — Task 10 bu rotayi tanimlar.

**Not (Task 6'dan devralinan duzeltmeler):** Bu projenin Expo Router koku
`mobil/app/` degil `mobil/src/app/` (create-expo-app sablonu boyle
olusturdu). Asagidaki dosya yollari ve `../../../lib/...` import derinligi
buna gore ayarlandi — `mobil/app/...` degil. Ayrica kurulu
`@testing-library/react-native@14.0.1` surumunde `render()` ve butun
`fireEvent.*` cagrilari **async** — asagidaki test kodunda hepsinin
onunde `await` var; bunu kaldirma.

- [ ] **Step 1: Basarisiz testi yaz**

`mobil/src/app/(auth)/dogrula.test.tsx`:

```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react-native'
import DogrulaEkrani from './dogrula'
import { supabase } from '../../../lib/supabase'

jest.mock('../../../lib/supabase', () => ({
  supabase: { auth: { verifyOtp: jest.fn(), signUp: jest.fn() } },
}))

const mockRouterReplace = jest.fn()
jest.mock('expo-router', () => ({
  useRouter: () => ({ replace: mockRouterReplace }),
  useLocalSearchParams: () => ({ telefon: '+905551234567' }),
}))

describe('DogrulaEkrani', () => {
  it('dogru kodla verifyOtp cagirir ve profil olusturmaya yonlendirir', async () => {
    ;(supabase.auth.verifyOtp as jest.Mock).mockResolvedValue({ data: {}, error: null })
    await render(<DogrulaEkrani />)
    await fireEvent.changeText(screen.getByPlaceholderText('Dogrulama kodu'), '123456')
    await fireEvent.press(screen.getByText('Dogrula'))
    await waitFor(() => {
      expect(supabase.auth.verifyOtp).toHaveBeenCalledWith({
        phone: '+905551234567',
        token: '123456',
        type: 'sms',
      })
    })
    expect(mockRouterReplace).toHaveBeenCalledWith('/profil-olustur')
  })

  it('yanlis kodda hata gosterir', async () => {
    ;(supabase.auth.verifyOtp as jest.Mock).mockResolvedValue({
      data: {},
      error: { message: 'Token has expired or is invalid' },
    })
    await render(<DogrulaEkrani />)
    await fireEvent.changeText(screen.getByPlaceholderText('Dogrulama kodu'), '000000')
    await fireEvent.press(screen.getByText('Dogrula'))
    await waitFor(() => {
      expect(screen.getByText('Token has expired or is invalid')).toBeTruthy()
    })
  })
})
```

- [ ] **Step 2: Basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- dogrula.test.tsx`
Expected: FAIL — `./dogrula` module bulunamiyor.

- [ ] **Step 3: Dogrulama ekranini yaz**

`mobil/src/app/(auth)/dogrula.tsx`:

```tsx
import { useState } from 'react'
import { View, Text, TextInput, Pressable, StyleSheet } from 'react-native'
import { useRouter, useLocalSearchParams } from 'expo-router'
import { supabase } from '../../../lib/supabase'

export default function DogrulaEkrani() {
  const router = useRouter()
  const { telefon } = useLocalSearchParams<{ telefon: string }>()
  const [kod, setKod] = useState('')
  const [hata, setHata] = useState<string | null>(null)
  const [gonderiliyor, setGonderiliyor] = useState(false)

  async function dogrula() {
    setHata(null)
    setGonderiliyor(true)
    const { error } = await supabase.auth.verifyOtp({
      phone: telefon,
      token: kod,
      type: 'sms',
    })
    setGonderiliyor(false)

    if (error) {
      setHata(error.message)
      return
    }
    router.replace('/profil-olustur')
  }

  return (
    <View style={stiller.kapsayici}>
      <Text style={stiller.baslik}>Telefonunu dogrula</Text>
      <Text style={stiller.aciklama}>{telefon} numarasina gonderilen kodu gir</Text>
      <TextInput
        style={stiller.girdi}
        placeholder="Dogrulama kodu"
        keyboardType="number-pad"
        value={kod}
        onChangeText={setKod}
      />
      {hata && <Text style={stiller.hata}>{hata}</Text>}
      <Pressable style={stiller.buton} onPress={dogrula} disabled={gonderiliyor}>
        <Text style={stiller.butonYazi}>{gonderiliyor ? 'Dogrulaniyor...' : 'Dogrula'}</Text>
      </Pressable>
    </View>
  )
}

const stiller = StyleSheet.create({
  kapsayici: { flex: 1, padding: 24, justifyContent: 'center' },
  baslik: { fontSize: 24, fontWeight: '600', marginBottom: 8 },
  aciklama: { color: '#555', marginBottom: 24 },
  girdi: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12, marginBottom: 12 },
  hata: { color: '#c00', marginBottom: 12 },
  buton: { backgroundColor: '#111', borderRadius: 8, padding: 14, alignItems: 'center' },
  butonYazi: { color: '#fff', fontWeight: '600' },
})
```

- [ ] **Step 4: Testlerin gectigini dogrula**

Run: `cd mobil && npm test -- dogrula.test.tsx`
Expected: PASS, 2 test.

- [ ] **Step 5: Commit**

```bash
cd ~/projects/cloud
git add mobil/src/app/\(auth\)/dogrula.tsx mobil/src/app/\(auth\)/dogrula.test.tsx
git commit -m "mobil: SMS dogrulama ekrani"
```

---

### Task 8: Giris ekrani

**Files:**
- Create: `mobil/src/app/(auth)/giris.tsx`
- Test: `mobil/src/app/(auth)/giris.test.tsx`

**Interfaces:**
- Consumes: `supabase` (Task 4), `eFormatinaCevir` (Task 5).
- Produces: basarili giristen sonra `/` rotasina yonlendirme (Task 9'daki kok
  yonlendirme mantigi devreye girer).

**Not (Task 6'dan devralinan duzeltmeler):** Router koku `mobil/src/app/`,
`mobil/app/` degil — yollar ve `../../../lib/...` import derinligi buna gore.
`@testing-library/react-native@14.0.1`'de `render()` ve `fireEvent.*` async —
asagida hepsinin onunde `await` var, kaldirma.

**Not (bu gorevin kendi hatasi):** Asagidaki ekran kodunda hem baslik hem
buton metni ayni ("Giris yap") — bu `getByText('Giris yap')`'i belirsiz
yapar (iki eslesme, test patlar). Baslik metnini `"Tekrar hos geldin"`
olarak yaz (kayit.tsx'teki "Hesap olustur" basligi / "Kayit ol" butonu
deseniyle tutarli), buton metnini degistirme — test dosyasi zaten
"Giris yap" buton metnine gore yazildi.

- [ ] **Step 1: Basarisiz testi yaz**

`mobil/src/app/(auth)/giris.test.tsx`:

```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react-native'
import GirisEkrani from './giris'
import { supabase } from '../../../lib/supabase'

jest.mock('../../../lib/supabase', () => ({
  supabase: { auth: { signInWithPassword: jest.fn() } },
}))

const mockRouterReplace = jest.fn()
jest.mock('expo-router', () => ({
  useRouter: () => ({ replace: mockRouterReplace }),
}))

describe('GirisEkrani', () => {
  it('dogru bilgilerle signInWithPassword cagirir ve yonlendirir', async () => {
    ;(supabase.auth.signInWithPassword as jest.Mock).mockResolvedValue({ data: {}, error: null })
    await render(<GirisEkrani />)
    await fireEvent.changeText(screen.getByPlaceholderText('05XX XXX XX XX'), '5551234567')
    await fireEvent.changeText(screen.getByPlaceholderText('Sifre'), 'sifre1234')
    await fireEvent.press(screen.getByText('Giris yap'))
    await waitFor(() => {
      expect(supabase.auth.signInWithPassword).toHaveBeenCalledWith({
        phone: '+905551234567',
        password: 'sifre1234',
      })
    })
    expect(mockRouterReplace).toHaveBeenCalledWith('/')
  })

  it('yanlis sifrede hata gosterir', async () => {
    ;(supabase.auth.signInWithPassword as jest.Mock).mockResolvedValue({
      data: {},
      error: { message: 'Invalid login credentials' },
    })
    await render(<GirisEkrani />)
    await fireEvent.changeText(screen.getByPlaceholderText('05XX XXX XX XX'), '5551234567')
    await fireEvent.changeText(screen.getByPlaceholderText('Sifre'), 'yanlissifre')
    await fireEvent.press(screen.getByText('Giris yap'))
    await waitFor(() => {
      expect(screen.getByText('Invalid login credentials')).toBeTruthy()
    })
  })
})
```

- [ ] **Step 2: Basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- giris.test.tsx`
Expected: FAIL — `./giris` module bulunamiyor.

- [ ] **Step 3: Giris ekranini yaz**

`mobil/src/app/(auth)/giris.tsx`:

```tsx
import { useState } from 'react'
import { View, Text, TextInput, Pressable, StyleSheet } from 'react-native'
import { useRouter } from 'expo-router'
import { supabase } from '../../../lib/supabase'
import { eFormatinaCevir } from '../../../lib/telefon'

export default function GirisEkrani() {
  const router = useRouter()
  const [telefon, setTelefon] = useState('')
  const [sifre, setSifre] = useState('')
  const [hata, setHata] = useState<string | null>(null)
  const [gonderiliyor, setGonderiliyor] = useState(false)

  async function girisYap() {
    setHata(null)
    const eFormatli = eFormatinaCevir(telefon)
    if (!eFormatli) {
      setHata('Gecerli bir telefon numarasi gir')
      return
    }

    setGonderiliyor(true)
    const { error } = await supabase.auth.signInWithPassword({ phone: eFormatli, password: sifre })
    setGonderiliyor(false)

    if (error) {
      setHata(error.message)
      return
    }
    router.replace('/')
  }

  return (
    <View style={stiller.kapsayici}>
      <Text style={stiller.baslik}>Tekrar hos geldin</Text>
      <TextInput
        style={stiller.girdi}
        placeholder="05XX XXX XX XX"
        keyboardType="phone-pad"
        value={telefon}
        onChangeText={setTelefon}
      />
      <TextInput
        style={stiller.girdi}
        placeholder="Sifre"
        secureTextEntry
        value={sifre}
        onChangeText={setSifre}
      />
      {hata && <Text style={stiller.hata}>{hata}</Text>}
      <Pressable style={stiller.buton} onPress={girisYap} disabled={gonderiliyor}>
        <Text style={stiller.butonYazi}>{gonderiliyor ? 'Giris yapiliyor...' : 'Giris yap'}</Text>
      </Pressable>
    </View>
  )
}

const stiller = StyleSheet.create({
  kapsayici: { flex: 1, padding: 24, justifyContent: 'center' },
  baslik: { fontSize: 24, fontWeight: '600', marginBottom: 24 },
  girdi: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12, marginBottom: 12 },
  hata: { color: '#c00', marginBottom: 12 },
  buton: { backgroundColor: '#111', borderRadius: 8, padding: 14, alignItems: 'center' },
  butonYazi: { color: '#fff', fontWeight: '600' },
})
```

- [ ] **Step 4: Testlerin gectigini dogrula**

Run: `cd mobil && npm test -- giris.test.tsx`
Expected: PASS, 2 test.

- [ ] **Step 5: Commit**

```bash
cd ~/projects/cloud
git add mobil/src/app/\(auth\)/giris.tsx mobil/src/app/\(auth\)/giris.test.tsx
git commit -m "mobil: giris ekrani"
```

---

### Task 9: Oturum context'i ve kok yonlendirme

**Files:**
- Create: `mobil/lib/oturum.tsx`
- Test: `mobil/lib/oturum.test.tsx`
- Modify: `mobil/src/app/_layout.tsx`

**Interfaces:**
- Consumes: `supabase` (Task 4), `public.profiller` tablosu (Task 3).
- Produces: `OturumSaglayici` (React context provider), `useOturum(): { oturum:
  Session | null, profilVarMi: boolean | null, yukleniyor: boolean }` — Task 12
  ve kok layout bunu kullanir. `profilVarMi`, oturum yoksa `null`; oturum varsa
  `profiller` tablosunda satir olup olmadigini yansitir. Bu, oturumu acik ama
  profilini yarim birakan bir kullanicinin uygulama yeniden acildiginda
  dogrudan ana ekrana degil, `/profil-olustur`'a yonlendirilmesini saglar.

**Not (Task 6'dan devralinan duzeltmeler):** Router koku `mobil/src/app/`,
`mobil/app/` degil. `mobil/src/app/_layout.tsx` **suanda create-expo-app'in
demo tab sablonuyla dolu** (`AppTabs`, `AnimatedSplashOverlay`, tema
saglayicisi) — bu adim onu asagidaki minimal, tab'siz surumle **tamamen
degistirir**, tema/tab kodunu korumaya calisma. Bu degisiklikten sonra
`src/app/index.tsx` (henuz Task 12'de degismedi) ve `src/app/explore.tsx`
gecici olarak "sahipsiz" kalir — hicbir tab navigator onlara baglanti
vermez ama dosyalar hala derlenir calisir durumda kalirlar. Bu beklenen bir
ara durum; onlari ve kullandiklari demo bilesenlerini (`src/components/*`,
`src/constants/theme.ts`, `src/hooks/use-theme.ts`,
`src/hooks/use-color-scheme*.ts`, `src/global.css`) silmek Task 12'nin isi
(index.tsx'i degistirdiginde), simdi degil. `@testing-library/react-native`'de
`render()` async — asagida `await` var, kaldirma.

- [ ] **Step 1: Basarisiz testi yaz**

`mobil/lib/oturum.test.tsx`:

```tsx
import { render, screen, waitFor } from '@testing-library/react-native'
import { Text } from 'react-native'
import { OturumSaglayici, useOturum } from './oturum'
import { supabase } from './supabase'

jest.mock('./supabase', () => ({
  supabase: {
    auth: {
      getSession: jest.fn(),
      onAuthStateChange: jest.fn(() => ({ data: { subscription: { unsubscribe: jest.fn() } } })),
    },
    from: jest.fn(),
  },
}))

function TestBileseni() {
  const { oturum, profilVarMi, yukleniyor } = useOturum()
  if (yukleniyor) return <Text>yukleniyor</Text>
  if (!oturum) return <Text>oturum-yok</Text>
  return <Text>{profilVarMi ? 'profil-var' : 'profil-yok'}</Text>
}

describe('OturumSaglayici', () => {
  it('oturum varsa profilin var olup olmadigini kontrol eder', async () => {
    ;(supabase.auth.getSession as jest.Mock).mockResolvedValue({
      data: { session: { user: { id: 'kullanici-1' } } },
    })
    ;(supabase.from as jest.Mock).mockReturnValue({
      select: jest.fn().mockReturnValue({
        eq: jest.fn().mockReturnValue({
          maybeSingle: jest.fn().mockResolvedValue({ data: { id: 'kullanici-1' } }),
        }),
      }),
    })
    await render(
      <OturumSaglayici>
        <TestBileseni />
      </OturumSaglayici>
    )
    await waitFor(() => {
      expect(screen.getByText('profil-var')).toBeTruthy()
    })
  })

  it('oturum yoksa profil sorgusu yapmaz', async () => {
    ;(supabase.auth.getSession as jest.Mock).mockResolvedValue({ data: { session: null } })
    await render(
      <OturumSaglayici>
        <TestBileseni />
      </OturumSaglayici>
    )
    await waitFor(() => {
      expect(screen.getByText('oturum-yok')).toBeTruthy()
    })
    expect(supabase.from).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: Basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- oturum.test.tsx`
Expected: FAIL — `./oturum` module bulunamiyor.

- [ ] **Step 3: Oturum context'ini yaz**

`mobil/lib/oturum.tsx`:

```tsx
import { createContext, useContext, useEffect, useState, type ReactNode } from 'react'
import type { Session } from '@supabase/supabase-js'
import { supabase } from './supabase'

type OturumDurumu = {
  oturum: Session | null
  profilVarMi: boolean | null
  yukleniyor: boolean
}

const OturumBaglami = createContext<OturumDurumu>({
  oturum: null,
  profilVarMi: null,
  yukleniyor: true,
})

async function profilVarMiKontrolEt(kullaniciId: string): Promise<boolean> {
  const { data } = await supabase
    .from('profiller')
    .select('id')
    .eq('id', kullaniciId)
    .maybeSingle()
  return data !== null
}

export function OturumSaglayici({ children }: { children: ReactNode }) {
  const [oturum, setOturum] = useState<Session | null>(null)
  const [profilVarMi, setProfilVarMi] = useState<boolean | null>(null)
  const [yukleniyor, setYukleniyor] = useState(true)

  useEffect(() => {
    async function baslangicOturumunuYukle() {
      const { data } = await supabase.auth.getSession()
      setOturum(data.session)
      if (data.session) {
        setProfilVarMi(await profilVarMiKontrolEt(data.session.user.id))
      }
      setYukleniyor(false)
    }
    baslangicOturumunuYukle()

    const { data: dinleyici } = supabase.auth.onAuthStateChange(async (_olay, yeniOturum) => {
      setOturum(yeniOturum)
      if (yeniOturum) {
        setProfilVarMi(await profilVarMiKontrolEt(yeniOturum.user.id))
      } else {
        setProfilVarMi(null)
      }
    })

    return () => dinleyici.subscription.unsubscribe()
  }, [])

  return (
    <OturumBaglami.Provider value={{ oturum, profilVarMi, yukleniyor }}>
      {children}
    </OturumBaglami.Provider>
  )
}

export function useOturum() {
  return useContext(OturumBaglami)
}
```

- [ ] **Step 4: Testlerin gectigini dogrula**

Run: `cd mobil && npm test -- oturum.test.tsx`
Expected: PASS, 2 test.

- [ ] **Step 5: Kok layout'u yonlendirmeyi yapacak sekilde guncelle**

`mobil/src/app/_layout.tsx` — asagidaki icerikle **tamamen degistir** (mevcut
tab/tema/splash kodunu sil):

```tsx
import { useEffect } from 'react'
import { Slot, useRouter, useSegments } from 'expo-router'
import { OturumSaglayici, useOturum } from '../../lib/oturum'

function YonlendirmeKontrolu() {
  const { oturum, profilVarMi, yukleniyor } = useOturum()
  const segments = useSegments()
  const router = useRouter()

  useEffect(() => {
    if (yukleniyor) return
    const authGrubunda = segments[0] === '(auth)'
    const profilOlusturEkraninda = segments[0] === 'profil-olustur'

    if (!oturum && !authGrubunda) {
      router.replace('/giris')
    } else if (oturum && profilVarMi === false && !profilOlusturEkraninda) {
      router.replace('/profil-olustur')
    } else if (oturum && profilVarMi && (authGrubunda || profilOlusturEkraninda)) {
      router.replace('/')
    }
  }, [oturum, profilVarMi, yukleniyor, segments])

  return <Slot />
}

export default function KokLayout() {
  return (
    <OturumSaglayici>
      <YonlendirmeKontrolu />
    </OturumSaglayici>
  )
}
```

`profilVarMi === null` durumu (henuz sorgu donmedi) kasitli olarak hicbir
yonlendirmeyi tetiklemiyor — yarim veriyle erken yonlendirme yapmamak icin.

- [ ] **Step 6: Web'de manuel dogrula**

Run: `cd mobil && npx expo start --web`
Expected: Oturum yokken `/giris`'e yonlendirir. Sunucuyu durdur.

- [ ] **Step 7: Commit**

```bash
cd ~/projects/cloud
git add mobil/lib/oturum.tsx mobil/lib/oturum.test.tsx mobil/src/app/_layout.tsx
git commit -m "mobil: oturum context'i ve kok yonlendirme mantigi"
```

---

### Task 10: Profil olusturma ekrani

**Files:**
- Create: `mobil/src/app/profil-olustur.tsx`
- Test: `mobil/src/app/profil-olustur.test.tsx`

**Interfaces:**
- Consumes: `supabase` (Task 4), `onSekizAltindaMi` (Task 5).
- Produces: basarili profil olusturmadan sonra `/` rotasina yonlendirme (Task
  12'nin ana ekrani); `profiller` tablosuna yeni satir ekler, bu da Task 9'daki
  `profilVarMi` kontrolunun bir sonraki oturumda `true` donmesini saglar.

**Not (Task 6'dan devralinan duzeltmeler):** Router koku `mobil/src/app/`.
Bu dosya `src/app/` altinda dogrudan durdugu icin (`_layout.tsx` gibi) `lib/`e
import derinligi `../../lib/...` (iki seviye). `render()`/`fireEvent.*` async —
asagida `await` var, kaldirma.

- [ ] **Step 1: Basarisiz testi yaz**

`mobil/src/app/profil-olustur.test.tsx`:

```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react-native'
import ProfilOlusturEkrani from './profil-olustur'
import { supabase } from '../../lib/supabase'

jest.mock('../../lib/supabase', () => ({
  supabase: {
    auth: { getUser: jest.fn().mockResolvedValue({ data: { user: { id: 'kullanici-1' } } }) },
    from: jest.fn(() => ({ insert: jest.fn().mockResolvedValue({ error: null }) })),
  },
}))

const mockRouterReplace = jest.fn()
jest.mock('expo-router', () => ({
  useRouter: () => ({ replace: mockRouterReplace }),
}))

describe('ProfilOlusturEkrani', () => {
  it('18 yas altinda dogum tarihinde hata gosterir', async () => {
    await render(<ProfilOlusturEkrani />)
    await fireEvent.changeText(screen.getByPlaceholderText('Adin'), 'Ada')
    await fireEvent.changeText(screen.getByPlaceholderText('YYYY-AA-GG'), '2015-01-01')
    await fireEvent.press(screen.getByText('Devam et'))
    await waitFor(() => {
      expect(screen.getByText('Uygulamayi kullanmak icin 18 yasinda olmalisin')).toBeTruthy()
    })
    expect(supabase.from).not.toHaveBeenCalled()
  })

  it('gecerli bilgilerle profil olusturur ve ana ekrana yonlendirir', async () => {
    await render(<ProfilOlusturEkrani />)
    await fireEvent.changeText(screen.getByPlaceholderText('Adin'), 'Ada')
    await fireEvent.changeText(screen.getByPlaceholderText('YYYY-AA-GG'), '2000-01-01')
    await fireEvent.changeText(screen.getByPlaceholderText('Kisa bir tanitim yaz'), 'Merhaba!')
    await fireEvent.press(screen.getByText('Devam et'))
    await waitFor(() => {
      expect(supabase.from).toHaveBeenCalledWith('profiller')
    })
    expect(mockRouterReplace).toHaveBeenCalledWith('/')
  })
})
```

- [ ] **Step 2: Basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- profil-olustur.test.tsx`
Expected: FAIL — `./profil-olustur` module bulunamiyor.

- [ ] **Step 3: Profil olusturma ekranini yaz**

`mobil/src/app/profil-olustur.tsx`:

```tsx
import { useState } from 'react'
import { View, Text, TextInput, Pressable, StyleSheet } from 'react-native'
import { useRouter } from 'expo-router'
import { supabase } from '../../lib/supabase'
import { onSekizAltindaMi } from '../../lib/yas'

export default function ProfilOlusturEkrani() {
  const router = useRouter()
  const [ad, setAd] = useState('')
  const [dogumTarihiMetni, setDogumTarihiMetni] = useState('')
  const [biyografi, setBiyografi] = useState('')
  const [hata, setHata] = useState<string | null>(null)
  const [gonderiliyor, setGonderiliyor] = useState(false)

  async function devamEt() {
    setHata(null)

    if (ad.trim().length === 0) {
      setHata('Adini yaz')
      return
    }

    const dogumTarihi = new Date(dogumTarihiMetni)
    if (isNaN(dogumTarihi.getTime())) {
      setHata('Gecerli bir dogum tarihi gir (YYYY-AA-GG)')
      return
    }
    if (onSekizAltindaMi(dogumTarihi)) {
      setHata('Uygulamayi kullanmak icin 18 yasinda olmalisin')
      return
    }

    setGonderiliyor(true)
    const { data: kullaniciVerisi } = await supabase.auth.getUser()
    const kullaniciId = kullaniciVerisi.user?.id

    const { error } = await supabase.from('profiller').insert({
      id: kullaniciId,
      ad: ad.trim(),
      dogum_tarihi: dogumTarihiMetni,
      biyografi: biyografi.trim() || null,
    })
    setGonderiliyor(false)

    if (error) {
      setHata(error.message)
      return
    }
    router.replace('/')
  }

  return (
    <View style={stiller.kapsayici}>
      <Text style={stiller.baslik}>Profilini olustur</Text>
      <TextInput style={stiller.girdi} placeholder="Adin" value={ad} onChangeText={setAd} />
      <TextInput
        style={stiller.girdi}
        placeholder="YYYY-AA-GG"
        value={dogumTarihiMetni}
        onChangeText={setDogumTarihiMetni}
      />
      <TextInput
        style={[stiller.girdi, stiller.cokSatirli]}
        placeholder="Kisa bir tanitim yaz"
        value={biyografi}
        onChangeText={setBiyografi}
        multiline
      />
      {hata && <Text style={stiller.hata}>{hata}</Text>}
      <Pressable style={stiller.buton} onPress={devamEt} disabled={gonderiliyor}>
        <Text style={stiller.butonYazi}>{gonderiliyor ? 'Kaydediliyor...' : 'Devam et'}</Text>
      </Pressable>
    </View>
  )
}

const stiller = StyleSheet.create({
  kapsayici: { flex: 1, padding: 24, justifyContent: 'center' },
  baslik: { fontSize: 24, fontWeight: '600', marginBottom: 24 },
  girdi: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12, marginBottom: 12 },
  cokSatirli: { height: 80, textAlignVertical: 'top' },
  hata: { color: '#c00', marginBottom: 12 },
  buton: { backgroundColor: '#111', borderRadius: 8, padding: 14, alignItems: 'center' },
  butonYazi: { color: '#fff', fontWeight: '600' },
})
```

- [ ] **Step 4: Testlerin gectigini dogrula**

Run: `cd mobil && npm test -- profil-olustur.test.tsx`
Expected: PASS, 2 test.

- [ ] **Step 5: Commit**

```bash
cd ~/projects/cloud
git add mobil/src/app/profil-olustur.tsx mobil/src/app/profil-olustur.test.tsx
git commit -m "mobil: profil olusturma ekrani, 18 yas kontrolu"
```

---

### Task 11: Profil fotografi — Storage bucket + RLS + yukleme UI

**Files:**
- Create: `mobil/supabase/migrations/<timestamp>_profil_fotograflari_bucket.sql`
- Create: `mobil/lib/fotograf-yukle.ts`
- Test: `mobil/lib/fotograf-yukle.test.ts`
- Modify: `mobil/src/app/profil-olustur.tsx`

**Interfaces:**
- Produces: `fotografYukle(kullaniciId: string, yerelUri: string): Promise<string>`
  (yuklenen dosyanin storage path'ini doner) — profil-olustur.tsx bunu cagirir.

**Not (Task 6'dan devralinan duzeltmeler):** `profil-olustur.tsx` artik
`mobil/src/app/profil-olustur.tsx`'te; oraya eklenecek
`import { fotografYukle } from '../lib/fotograf-yukle'` satiri
`'../../lib/fotograf-yukle'` olmali (iki seviye).

**Not (Supabase baglantisi):** Step 2 (`supabase db push`) gercek, linkli
bir Supabase projesi gerektirir. Eger Task 2 (hesap/proje olusturma) henuz
kullanici tarafindan tamamlanmadiysa, Step 1'i (migrasyon dosyasini yazma)
yap ama Step 2'yi atla ve pending olarak isaretle — kod ve testler
(Step 3-9) buna bagli degil, calisir.

- [ ] **Step 1: Bucket migrasyonunu yaz**

```bash
cd ~/projects/cloud/mobil
supabase migration new profil_fotograflari_bucket
```

Dosyanin icerigi:

```sql
insert into storage.buckets (id, name, public)
values ('profil-fotograflari', 'profil-fotograflari', false);

create policy "kendi fotografini yukleyebilir"
  on storage.objects for insert
  with check (
    bucket_id = 'profil-fotograflari'
    and (storage.foldername(name))[1] = auth.uid()::text
  );

create policy "kendi fotografini okuyabilir"
  on storage.objects for select
  using (
    bucket_id = 'profil-fotograflari'
    and (storage.foldername(name))[1] = auth.uid()::text
  );
```

- [ ] **Step 2: Migrasyonu uygula**

```bash
supabase db push
```

Expected: hatasiz uygulanir. Dashboard → Storage'da `profil-fotograflari`
bucket'i gorunur.

- [ ] **Step 3: Bagimliliklari kur**

```bash
npx expo install expo-image-picker expo-file-system
npm install base64-arraybuffer
```

- [ ] **Step 4: Basarisiz testi yaz**

`mobil/lib/fotograf-yukle.test.ts`:

```ts
import { fotografYukle } from './fotograf-yukle'
import { supabase } from './supabase'
import * as FileSystem from 'expo-file-system'

jest.mock('./supabase', () => ({
  supabase: { storage: { from: jest.fn() } },
}))
jest.mock('expo-file-system', () => ({
  readAsStringAsync: jest.fn().mockResolvedValue('base64icerik'),
  EncodingType: { Base64: 'base64' },
}))

describe('fotografYukle', () => {
  it('dosyayi kullanici klasorune yukler ve path doner', async () => {
    const uploadMock = jest.fn().mockResolvedValue({ data: { path: 'kullanici-1/123.jpg' }, error: null })
    ;(supabase.storage.from as jest.Mock).mockReturnValue({ upload: uploadMock })

    const sonuc = await fotografYukle('kullanici-1', 'file:///gecici/foto.jpg')

    expect(FileSystem.readAsStringAsync).toHaveBeenCalledWith('file:///gecici/foto.jpg', {
      encoding: 'base64',
    })
    expect(uploadMock).toHaveBeenCalledWith(
      expect.stringMatching(/^kullanici-1\/\d+\.jpg$/),
      expect.anything(),
      { contentType: 'image/jpeg' }
    )
    expect(sonuc).toBe('kullanici-1/123.jpg')
  })
})
```

- [ ] **Step 5: Basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- fotograf-yukle.test.ts`
Expected: FAIL — `./fotograf-yukle` module bulunamiyor.

- [ ] **Step 6: Yukleme fonksiyonunu yaz**

`mobil/lib/fotograf-yukle.ts`:

```ts
import * as FileSystem from 'expo-file-system'
import { decode } from 'base64-arraybuffer'
import { supabase } from './supabase'

export async function fotografYukle(kullaniciId: string, yerelUri: string): Promise<string> {
  const base64 = await FileSystem.readAsStringAsync(yerelUri, {
    encoding: FileSystem.EncodingType.Base64,
  })

  const dosyaYolu = `${kullaniciId}/${Date.now()}.jpg`
  const { data, error } = await supabase.storage
    .from('profil-fotograflari')
    .upload(dosyaYolu, decode(base64), { contentType: 'image/jpeg' })

  if (error) throw error
  return data.path
}
```

- [ ] **Step 7: Testin gectigini dogrula**

Run: `cd mobil && npm test -- fotograf-yukle.test.ts`
Expected: PASS.

- [ ] **Step 8: Profil olusturma ekranina fotograf secimi ekle**

`mobil/src/app/profil-olustur.tsx` icine, mevcut state'lerin yanina ve JSX'e ekle:

```tsx
// ustteki import'lara ekle
import * as ImagePicker from 'expo-image-picker'
import { fotografYukle } from '../../lib/fotograf-yukle'

// bileşen icinde, diger useState'lerin yanina
const [fotografUrileri, setFotografUrileri] = useState<string[]>([])

async function fotografEkle() {
  const sonuc = await ImagePicker.launchImageLibraryAsync({
    mediaTypes: ImagePicker.MediaTypeOptions.Images,
    quality: 0.7,
    allowsMultipleSelection: true,
    selectionLimit: 6,
  })
  if (!sonuc.canceled) {
    setFotografUrileri((mevcut) => [...mevcut, ...sonuc.assets.map((a) => a.uri)].slice(0, 6))
  }
}
```

`devamEt` fonksiyonunun `insert` cagrisindan once, her secilen fotografi sirayla
yukle (spec "birkac fotograf" diyor — tek fotografla sinirlamiyoruz):

```tsx
const fotografYollari: string[] = []
for (const uri of fotografUrileri) {
  const yol = await fotografYukle(kullaniciId!, uri)
  fotografYollari.push(yol)
}
```

`insert` cagrisina `fotograflar` alanini ekle:

```tsx
const { error } = await supabase.from('profiller').insert({
  id: kullaniciId,
  ad: ad.trim(),
  dogum_tarihi: dogumTarihiMetni,
  biyografi: biyografi.trim() || null,
  fotograflar: fotografYollari,
})
```

JSX'e, biyografi girdisinden once bir fotograf secme butonu ve secilen sayi
gostergesi ekle:

```tsx
<Pressable style={stiller.fotografButonu} onPress={fotografEkle}>
  <Text>
    {fotografUrileri.length > 0
      ? `${fotografUrileri.length} fotograf secildi`
      : 'Fotograf ekle'}
  </Text>
</Pressable>
```

`stiller` objesine ekle: `fotografButonu: { padding: 12, borderWidth: 1, borderColor: '#ccc', borderRadius: 8, marginBottom: 12, alignItems: 'center' }`.

- [ ] **Step 9: Mevcut profil-olustur testlerinin hala gectigini dogrula**

Run: `cd mobil && npm test -- profil-olustur.test.tsx fotograf-yukle.test.ts`
Expected: PASS (mevcut 2 test + yeni 1 test = 3 test).

- [ ] **Step 10: Commit**

```bash
cd ~/projects/cloud
git add mobil/supabase/migrations mobil/lib/fotograf-yukle.ts mobil/lib/fotograf-yukle.test.ts mobil/src/app/profil-olustur.tsx mobil/package.json mobil/package-lock.json
git commit -m "mobil: profil fotografi yukleme (Storage bucket + RLS + UI)"
```

---

### Task 12: Ana ekran ve cikis yap

**Files:**
- Modify (tamamen degistir): `mobil/src/app/index.tsx` — create-expo-app'in
  demo tanitim ekrani icerigini tasir, bu adim onu gercek "Hesabin hazir"
  ekraniyla degistirir.
- Test: `mobil/src/app/index.test.tsx`
- Delete: `mobil/src/app/explore.tsx` ve onu/`index.tsx`'in eski halini
  besleyen, artik hicbir yerden kullanilmayan demo kumesi (asagida Step 5'te
  tam liste).

**Interfaces:**
- Consumes: `supabase` (Task 4), `useOturum` (Task 9).

**Not (Task 6'dan devralinan duzeltmeler):** Router koku `mobil/src/app/`.
`render()`/`fireEvent.*` async — asagida `await` var, kaldirma.

- [ ] **Step 1: Basarisiz testi yaz**

`mobil/src/app/index.test.tsx`:

```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react-native'
import AnaEkran from './index'
import { supabase } from '../../lib/supabase'

jest.mock('../../lib/supabase', () => ({
  supabase: { auth: { signOut: jest.fn().mockResolvedValue({ error: null }) } },
}))

describe('AnaEkran', () => {
  it('cikis yap butonuna basinca signOut cagirir', async () => {
    await render(<AnaEkran />)
    await fireEvent.press(screen.getByText('Cikis yap'))
    await waitFor(() => {
      expect(supabase.auth.signOut).toHaveBeenCalled()
    })
  })
})
```

- [ ] **Step 2: Basarisiz oldugunu dogrula**

Run: `cd mobil && npm test -- index.test.tsx`
Expected: FAIL — mevcut `index.tsx` (create-expo-app'in demo tanitim ekrani)
yuzunden `'Cikis yap'` metni bulunamiyor.

- [ ] **Step 3: Ana ekrani yaz**

`mobil/src/app/index.tsx` icerigini **tamamen** asagidakiyle degistir (demo
tanitim ekraninin butun kodunu sil):

```tsx
import { View, Text, Pressable, StyleSheet } from 'react-native'
import { supabase } from '../../lib/supabase'

export default function AnaEkran() {
  async function cikisYap() {
    await supabase.auth.signOut()
  }

  return (
    <View style={stiller.kapsayici}>
      <Text style={stiller.baslik}>Hesabin hazir</Text>
      <Text style={stiller.aciklama}>
        Yakinda cevrendeki mekanlari ve insanlari burada gorecegin.
      </Text>
      <Pressable style={stiller.buton} onPress={cikisYap}>
        <Text style={stiller.butonYazi}>Cikis yap</Text>
      </Pressable>
    </View>
  )
}

const stiller = StyleSheet.create({
  kapsayici: { flex: 1, padding: 24, justifyContent: 'center', alignItems: 'center' },
  baslik: { fontSize: 24, fontWeight: '600', marginBottom: 8 },
  aciklama: { color: '#555', textAlign: 'center', marginBottom: 24 },
  buton: { backgroundColor: '#111', borderRadius: 8, padding: 14, alignItems: 'center' },
  butonYazi: { color: '#fff', fontWeight: '600' },
})
```

- [ ] **Step 4: Testin gectigini dogrula**

Run: `cd mobil && npm test -- index.test.tsx`
Expected: PASS.

- [ ] **Step 5: Artik kullanilmayan demo dosyalarini sil**

Task 9, `_layout.tsx`'i tab navigator'suz bir surumle degistirdiginde
`src/app/explore.tsx` ve `src/app/index.tsx`'in eski (demo) icerigi
sahipsiz kalmisti. Bu adimda `index.tsx`'in gercek icerigi geldigine gore,
artik hicbir dosyadan referans alinmayan butun demo kumesini sil:

```bash
cd mobil
rm -rf src/app/explore.tsx
rm -rf src/components/animated-icon.tsx src/components/animated-icon.web.tsx
rm -rf src/components/app-tabs.tsx src/components/app-tabs.web.tsx
rm -rf src/components/external-link.tsx src/components/hint-row.tsx
rm -rf src/components/themed-text.tsx src/components/themed-view.tsx
rm -rf src/components/web-badge.tsx src/components/ui
rm -rf src/constants/theme.ts
rm -rf src/hooks/use-color-scheme.ts src/hooks/use-color-scheme.web.ts src/hooks/use-theme.ts
rm -rf src/global.css
```

Dogrula: `grep -rn "components/\|constants/theme\|hooks/use-theme\|hooks/use-color-scheme\|global.css" src/app src/components src/hooks src/constants 2>/dev/null` bos donmeli (hicbir kalan dosya bunlari import etmiyor olmali). Eger bir
sey bulursa o dosyayi silme, sadece raporla.

Full test suite'i tekrar calistir, pristine oldugunu dogrula:

Run: `cd mobil && npm test`
Expected: PASS, onceki adimlarin butun testleri (14 test civari) yesil.

- [ ] **Step 6: Commit**

```bash
cd ~/projects/cloud
git add mobil/src/app/index.tsx mobil/src/app/index.test.tsx
git add -u mobil/src/app/explore.tsx mobil/src/components mobil/src/constants mobil/src/hooks
git commit -m "mobil: ana ekran (hesap hazir), cikis yap, kullanilmayan demo kodunu sil"
```

---

### Task 13: Ucretsiz test telefon numaralariyla uctan uca dogrulama

**Files:**
- Modify: yok (yalnizca Supabase dashboard ayari + manuel dogrulama)

**Interfaces:**
- Tuketir: Task 6-12'de yazilan tum ekranlar.

- [ ] **Step 1: Dashboard'da test telefon numarasi tanimla**

Supabase Dashboard → Authentication → Sign In / Providers → Phone → "Test OTP"
bolumune git (arayuz etiketleri surumden suruma degisebilir; "Phone" saglayici
ayarlarinin altinda "test" gecen bir bolum ara). Bir test numarasi ve sabit kod
ekle, ornegin numara `+905550000000`, kod `123456`. Bu numaraya gercek SMS
gitmez, ucret cikmaz.

- [ ] **Step 2: Web'de tam akisi calistir**

```bash
cd ~/projects/cloud/mobil
npx expo start --web
```

Tarayicida sirasiyla:
1. `/kayit` → telefon: `5550000000`, sifre: `test1234` → "Kayit ol"
2. `/dogrula` ekraninda kod: `123456` → "Dogrula"
3. `/profil-olustur` ekraninda ad, dogum tarihi (18+), biyografi gir → "Devam et"
4. `/` ekraninda "Hesabin hazir" gorunmeli.
5. "Cikis yap" → `/giris`'e donmeli.
6. `/giris` ekraninda ayni telefon+sifre ile tekrar giris yap → dogrudan `/`'e
   gitmeli (profil tekrar sorulmamali).

Expected: Her adim hatasiz gecer, gercek SMS gonderilmez.

- [ ] **Step 3: Veritabanini dogrula**

Dashboard → Table Editor → `profiller` tablosunda yeni satirin olustugunu,
`dogum_tarihi` ve `ad` alanlarinin dogru kaydedildigini kontrol et.

- [ ] **Step 4: 18 yas alti reddini dogrula**

`/kayit` ile yeni bir test numarasiyla (ornegin `+905550000001`, dashboard'a
ayni sekilde eklenir) kayit ol, dogrula, profil olusturma ekraninda dogum
tarihi olarak bugunden 17 yil oncesini gir. Expected: "Uygulamayi kullanmak
icin 18 yasinda olmalisin" hatasi gorunur, `profiller` tablosuna satir eklenmez.

- [ ] **Step 5: Konusma gunlugune sonucu yaz**

`docs/konusma-gunlugu.md` dosyasina bir giris ekle: Faz 1 uctan uca akisin
dogrulandigi, hangi tarihte ve hangi ortamda (web) test edildigi.

- [ ] **Step 6: Commit**

```bash
cd ~/projects/cloud
git add docs/konusma-gunlugu.md
git commit -m "docs: Faz 1 uctan uca akis dogrulandi"
```

---

### Task 14: (izole, deploy-time) Twilio prod SMS saglayicisi

**Ne zaman calistirilir:** Yalnizca uygulama gercek kullanicilara acilmadan
hemen once. Faz 1 gelistirmesi ve testi bu gorev olmadan tamamen calisir —
Task 13 test numaralariyla dogrulanir.

**Files:**
- Modify: yok (yalnizca Supabase dashboard + Twilio hesap ayari)

- [ ] **Step 1: Twilio hesabi ac**

<https://www.twilio.com> uzerinde hesap ac. Yeni hesaplara ~15$ deneme
kredisi tanimlanir. "Buy a phone number" ile SMS gonderebilen bir numara al
(deneme hesabinda ucretsiz bir numara secilebilir).

- [ ] **Step 2: Supabase'e Twilio bilgilerini gir**

Dashboard → Authentication → Providers → Phone → SMS Provider olarak
"Twilio" sec. Twilio Console'dan alinan `Account SID`, `Auth Token` ve
gonderen numarayi gir. Kaydet.

- [ ] **Step 3: Test numaralarini kapat (opsiyonel ama onerilir)**

Dashboard → Authentication → Providers → Phone → "Test OTP" bolumundeki test
numaralarini prod'a cikmadan once sil, boylece gercek kullanicilarla
karismalar.

- [ ] **Step 4: Gercek bir numarayla dogrula**

Kendi telefon numarani kullanarak `/kayit` akisini calistir, gercek SMS'in
geldigini dogrula. Twilio Console → Monitor → Logs'ta gonderilen mesaji
kontrol et.

- [ ] **Step 5: Konusma gunlugune ve CLAUDE.md'ye kaydet**

`docs/konusma-gunlugu.md` ve `CLAUDE.md` "Kararlar" bolumune Twilio'nun
devreye alindigi tarihi ve hangi Twilio numarasinin kullanildigini (son 4
hane yeterli, tam numara yazma) ekle.

- [ ] **Step 6: Commit**

```bash
cd ~/projects/cloud
git add docs/konusma-gunlugu.md CLAUDE.md
git commit -m "docs: Twilio prod SMS saglayicisi devreye alindi"
```
